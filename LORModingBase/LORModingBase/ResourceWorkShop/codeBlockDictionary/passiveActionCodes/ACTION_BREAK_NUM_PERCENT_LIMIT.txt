if(this.owner != null)
{
    int <<0>> = this.owner.MaxBreakLife / 100 * {{0}};
    if (<<0>> > {{1}})
        <<0>> = {{1}};
    this.owner.ShowPassiveTypo(this);
    this.owner.RecoverBreakLife(<<0>>);
}